<h2>New Tweet!</h2>
<form data-tweets-ul="#feed" class="tweet-compose" action="/tweets" method="POST">
  <strong class="chars-left">0/140</strong>
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <label>
    Content
    <textarea name="tweet[content]"></textarea>
  </label>
  <br>

  <label>
    Mention

    <a href="javascript:void(0)" class= "add-mentioned-user">Add Mention</a>

    <div class="mentioned-users">

    </div>


  </label>
  <br>

  <input type="Submit" value="Post Tweet!">

  <script type="text/template">
    <div class="single-mention">
      <a class="remove-mentioned-user" href="bogusland">Remove Mention</a>
      <select name="tweet[mentioned_user_ids][]">
        <option></option>
        <% User.all.each do |user| %>
        <option value="<%= user.id %>"><%= user.username %></option>
        <% end %>
      </select>
    </div>
  </script>

</form>
